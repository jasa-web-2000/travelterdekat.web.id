<div class="flex items-center py-3">
    <a class="block overflow-hidden hover:rotate-3" href="/" title="{{ web()->title }}">
        <img class="w-20 h-10" title="{{ web()->title }}" src="{{ web()->transparentLogo }}" alt="Logo {{ web()->title }}"
            width="80" height="40" />
    </a>
</div>
